name: Deploy to Azure

on:
  push:
    branches: [main]
  workflow_dispatch:

# Prevent multiple deployments from running simultaneously
# This ensures only one deployment runs at a time, queuing others
concurrency:
  group: production-deployment
  cancel-in-progress: false  # Don't cancel running deployments, queue them instead

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    name: Build and Deploy HR Portal
    runs-on: ubuntu-latest
    env:
      RESOURCE_GROUP: baynunah-hr-portal-rg
      WEBAPP_NAME: hrportal-backend-new
      WEBAPP_URL: https://hrportal-backend-new.azurewebsites.net

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate required secrets
        run: |
          echo "🔍 Validating required secrets..."
          if [ -z "${{ secrets.AZURE_CLIENT_ID }}" ] || \
             [ -z "${{ secrets.AZURE_TENANT_ID }}" ] || \
             [ -z "${{ secrets.AZURE_SUBSCRIPTION_ID }}" ]; then
            echo "❌ ERROR: Azure OIDC secrets missing"
            exit 1
          fi
          if [ -z "${{ secrets.DATABASE_URL }}" ]; then
            echo "❌ ERROR: DATABASE_URL secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.AUTH_SECRET_KEY }}" ]; then
            echo "❌ ERROR: AUTH_SECRET_KEY secret is not set"
            exit 1
          fi
          echo "✅ All required secrets present"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install and build frontend
        working-directory: frontend
        run: |
          npm install
          npm run build
        env:
          VITE_API_BASE_URL: /api

      - name: Copy frontend build to backend/static
        run: |
          rm -rf backend/static
          cp -r frontend/dist backend/static
          echo "✅ Frontend built to backend/static"
          ls -la backend/static/

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Create deployment package
        run: |
          cd backend
          
          # Generate build info for version tracking
          echo "Generating build info..."
          cat > build_info.txt << EOF
          GIT_COMMIT_SHA=${{ github.sha }}
          BUILD_TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          GITHUB_RUN_ID=${{ github.run_id }}
          GITHUB_RUN_NUMBER=${{ github.run_number }}
          DEPLOYED_BY=${{ github.actor }}
          EOF
          
          cat build_info.txt
          
          chmod +x azure_startup.sh
          zip -r ../deploy.zip . \
            -x "*.pyc" \
            -x "*__pycache__*" \
            -x "*.git*" \
            -x "*.env*" \
            -x "*test*.py" \
            -x "venv/*"
          cd ..
          echo "✅ Package created: $(du -h deploy.zip)"

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Pre-deployment Health Check
        run: |
          echo "🔍 Running pre-deployment health check..."
          
          # Check if App Service is running and responsive
          APP_STATE=$(az webapp show \
            --name "$WEBAPP_NAME" \
            --resource-group "$RESOURCE_GROUP" \
            --query "state" -o tsv)
          
          echo "App Service state: $APP_STATE"
          
          if [ "$APP_STATE" != "Running" ]; then
            echo "⚠️ App Service is not in Running state, attempting to start..."
            az webapp start --name "$WEBAPP_NAME" --resource-group "$RESOURCE_GROUP"
            echo "⏳ Waiting 30s for app to start..."
            sleep 30
          fi
          
          # Check if SCM site is accessible
          echo "🔍 Verifying SCM site accessibility..."
          SCM_CHECK=$(curl -s -o /dev/null -w "%{http_code}" \
            --connect-timeout 10 \
            --max-time 20 \
            "https://$WEBAPP_NAME.scm.azurewebsites.net/" 2>/dev/null || echo "000")
          
          if [ "$SCM_CHECK" = "200" ] || [ "$SCM_CHECK" = "401" ]; then
            echo "✅ SCM site is accessible (HTTP $SCM_CHECK)"
          else
            echo "⚠️ SCM site returned HTTP $SCM_CHECK, but continuing..."
          fi
          
          echo "✅ Pre-deployment health check complete"

      - name: Configure App Settings
        run: |
          ALLOWED_ORIGINS="${{ secrets.ALLOWED_ORIGINS }}"
          if [ -z "$ALLOWED_ORIGINS" ]; then
            ALLOWED_ORIGINS="$WEBAPP_URL"
          fi

          az webapp config appsettings set \
            --name "$WEBAPP_NAME" \
            --resource-group "$RESOURCE_GROUP" \
            --settings \
              DATABASE_URL="${{ secrets.DATABASE_URL }}" \
              AUTH_SECRET_KEY="${{ secrets.AUTH_SECRET_KEY }}" \
              ALLOWED_ORIGINS="$ALLOWED_ORIGINS" \
              APP_ENV="production" \
              GIT_COMMIT_SHA="${{ github.sha }}" \
              BUILD_TIMESTAMP="$(date -u +"%Y-%m-%d %H:%M:%S UTC")" \
              APP_VERSION="${{ github.run_number }}" \
              SCM_DO_BUILD_DURING_DEPLOYMENT="false" \
              ENABLE_ORYX_BUILD="true" \
              WEBSITE_RUN_FROM_PACKAGE="0" \
            --output none

          az webapp config set \
            --name "$WEBAPP_NAME" \
            --resource-group "$RESOURCE_GROUP" \
            --startup-file "./azure_startup.sh" \
            --output none

          echo "✅ App settings configured with version tracking"
          
          # Wait for SCM container to stabilize after configuration changes
          # Azure best practice: Wait 120-180s after config changes for SCM container to fully restart
          # This prevents "SCM container restart" errors during deployment
          echo "⏳ Waiting 150s for SCM container to stabilize after config changes..."
          sleep 150
          
          # Verify SCM container is ready before deployment
          echo "🔍 Checking SCM container readiness..."
          MAX_SCM_CHECKS=5
          SCM_CHECK=1
          SCM_READY=false
          
          while [ $SCM_CHECK -le $MAX_SCM_CHECKS ]; do
            echo "SCM readiness check $SCM_CHECK/$MAX_SCM_CHECKS..."
            SCM_STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              --connect-timeout 10 \
              --max-time 30 \
              "https://$WEBAPP_NAME.scm.azurewebsites.net/api/settings" 2>/dev/null || echo "000")
            
            if [ "$SCM_STATUS" = "200" ] || [ "$SCM_STATUS" = "401" ]; then
              echo "✅ SCM container is ready (HTTP $SCM_STATUS)"
              SCM_READY=true
              break
            fi
            
            if [ $SCM_CHECK -lt $MAX_SCM_CHECKS ]; then
              echo "⏳ SCM not ready yet (HTTP $SCM_STATUS), waiting 30s..."
              sleep 30
            fi
            SCM_CHECK=$((SCM_CHECK + 1))
          done
          
          if [ "$SCM_READY" = "false" ]; then
            echo "⚠️ SCM container readiness uncertain, proceeding with deployment..."
          fi

      - name: Deploy to Azure App Service
        run: |
          echo "🚀 Deploying to Azure App Service..."
          
          # Deploy using Azure CLI with enhanced retry logic
          MAX_ATTEMPTS=4
          ATTEMPT=1
          DEPLOYMENT_SUCCESS=false
          
          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
            echo "📦 Deployment attempt $ATTEMPT of $MAX_ATTEMPTS..."
            echo "⏰ Start time: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
            
            # Primary method: az webapp deploy with increased timeout
            # Increased from 600s to 900s (15 minutes) for Oryx build + large deployments
            az webapp deploy \
              --name "$WEBAPP_NAME" \
              --resource-group "$RESOURCE_GROUP" \
              --src-path deploy.zip \
              --type zip \
              --clean true \
              --restart true \
              --timeout 900 \
              --verbose 2>&1 | tee /tmp/deploy_output.txt || true
            DEPLOY_EXIT_CODE=${PIPESTATUS[0]}
            
            echo "⏰ End time: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
            echo "📋 Exit code: $DEPLOY_EXIT_CODE"
            
            if [ $DEPLOY_EXIT_CODE -eq 0 ]; then
              echo "✅ Deployment successful via az webapp deploy on attempt $ATTEMPT"
              DEPLOYMENT_SUCCESS=true
              rm -f /tmp/deploy_output.txt 2>/dev/null || true
              break
            else
              echo "⚠️ az webapp deploy attempt $ATTEMPT failed (exit code: $DEPLOY_EXIT_CODE)"
              
              # Enhanced error pattern detection
              # Priority order: SCM restart > Lock > Timeout > Unknown
              ERROR_TYPE=""
              if grep -qi "SCM container restart" /tmp/deploy_output.txt 2>/dev/null; then
                ERROR_TYPE="SCM_RESTART"
                echo "🔄 Detected SCM container restart issue (highest priority)"
              elif grep -qi "conflict\|locked" /tmp/deploy_output.txt 2>/dev/null; then
                ERROR_TYPE="LOCK"
                echo "🔒 Detected deployment lock/conflict"
              elif grep -qi "timeout\|timed out" /tmp/deploy_output.txt 2>/dev/null; then
                ERROR_TYPE="TIMEOUT"
                echo "⏰ Detected timeout issue"
              else
                ERROR_TYPE="UNKNOWN"
                echo "❓ Unknown error type"
              fi
              
              # Try fallback method on last attempt if primary method keeps failing
              if [ $ATTEMPT -eq $MAX_ATTEMPTS ]; then
                echo "🔄 Trying fallback: Kudu REST API deployment..."
                
                # Get publishing credentials
                CREDS=$(az webapp deployment list-publishing-credentials \
                  --name "$WEBAPP_NAME" \
                  --resource-group "$RESOURCE_GROUP" \
                  --query "{username:publishingUserName, password:publishingPassword}" \
                  -o json 2>/dev/null || echo '{}')
                
                USERNAME=$(echo $CREDS | jq -r '.username // empty')
                PASSWORD=$(echo $CREDS | jq -r '.password // empty')
                
                # Mask password in logs for security
                if [ -n "$PASSWORD" ]; then
                  echo "::add-mask::$PASSWORD"
                fi
                
                if [ -n "$USERNAME" ]; then
                  echo "📤 Uploading via Kudu API..."
                  KUDU_RESPONSE=$(curl -s -w "\n%{http_code}" \
                    --connect-timeout 60 \
                    --max-time 600 \
                    -X POST \
                    -u "$USERNAME:$PASSWORD" \
                    -H "Content-Type: application/zip" \
                    --data-binary @deploy.zip \
                    "https://$WEBAPP_NAME.scm.azurewebsites.net/api/zipdeploy?isAsync=false" || echo "000")
                  
                  KUDU_HTTP_CODE=$(echo "$KUDU_RESPONSE" | tail -1)
                  
                  if [ "$KUDU_HTTP_CODE" = "200" ] || [ "$KUDU_HTTP_CODE" = "202" ]; then
                    echo "✅ Fallback deployment via Kudu API successful!"
                    DEPLOYMENT_SUCCESS=true
                    # Restart app after Kudu deployment
                    az webapp restart --name "$WEBAPP_NAME" --resource-group "$RESOURCE_GROUP" || true
                    break
                  else
                    echo "❌ Kudu API deployment also failed (HTTP $KUDU_HTTP_CODE)"
                  fi
                else
                  echo "❌ Could not get publishing credentials for fallback method"
                fi
              fi
              
              if [ $ATTEMPT -lt $MAX_ATTEMPTS ]; then
                # Enhanced backoff strategy based on error type
                # Progressive backoff based on error type and attempt number
                if [ "$ERROR_TYPE" = "SCM_RESTART" ]; then
                  BASE_WAIT=120  # SCM needs longer stabilization
                  WAIT_TIME=$((BASE_WAIT + (ATTEMPT - 1) * 30))  # Add 30s per attempt
                elif [ "$ERROR_TYPE" = "TIMEOUT" ]; then
                  BASE_WAIT=60   # Timeout might just need retry
                  WAIT_TIME=$((BASE_WAIT + (ATTEMPT - 1) * 20))  # Add 20s per attempt
                elif [ "$ERROR_TYPE" = "LOCK" ]; then
                  BASE_WAIT=90   # Wait for lock to clear
                  WAIT_TIME=$((BASE_WAIT + (ATTEMPT - 1) * 30))  # Add 30s per attempt
                else
                  WAIT_TIME=$((60 * ATTEMPT))  # Exponential backoff for unknown
                fi
                
                echo "⏳ Waiting ${WAIT_TIME}s before retry..."
                sleep $WAIT_TIME
              else
                echo "❌ All deployment attempts failed (including fallback)"
                echo "📋 Deployment output (first 30 + last 70 lines for diagnostics):"
                if [ -f /tmp/deploy_output.txt ]; then
                  echo "=== First 30 lines ==="
                  head -30 /tmp/deploy_output.txt 2>/dev/null || true
                  echo ""
                  echo "=== Last 70 lines ==="
                  tail -70 /tmp/deploy_output.txt 2>/dev/null || true
                fi
                rm -f /tmp/deploy_output.txt 2>/dev/null || true
              fi
            fi
            
            ATTEMPT=$((ATTEMPT + 1))
          done
          
          if [ "$DEPLOYMENT_SUCCESS" = "false" ]; then
            echo "❌ DEPLOYMENT FAILED after all attempts"
            exit 1
          fi

      - name: Wait for deployment and Oryx build
        run: |
          echo "⏳ Waiting 120s for deployment and Oryx build to complete..."
          sleep 120

      - name: Run database migrations
        run: |
          echo "🔄 Running database migrations via Kudu..."

          # Get publishing credentials with retry logic
          MAX_CRED_ATTEMPTS=3
          CRED_ATTEMPT=1
          
          while [ $CRED_ATTEMPT -le $MAX_CRED_ATTEMPTS ]; do
            echo "📋 Getting publishing credentials (attempt $CRED_ATTEMPT)..."
            CREDS=$(az webapp deployment list-publishing-credentials \
              --name "$WEBAPP_NAME" \
              --resource-group "$RESOURCE_GROUP" \
              --query "{username:publishingUserName, password:publishingPassword}" \
              -o json 2>&1)
            
            if echo "$CREDS" | jq -e '.username' > /dev/null 2>&1; then
              break
            fi
            
            echo "⚠️ Failed to get credentials, retrying in 30s..."
            sleep 30
            CRED_ATTEMPT=$((CRED_ATTEMPT + 1))
          done

          USERNAME=$(echo $CREDS | jq -r '.username')
          PASSWORD=$(echo $CREDS | jq -r '.password')
          
          if [ -z "$USERNAME" ] || [ "$USERNAME" = "null" ]; then
            echo "❌ Failed to get publishing credentials after $MAX_CRED_ATTEMPTS attempts"
            echo "⚠️ Continuing without migrations - they will run on app startup"
            exit 0
          fi

          # Run migration command via Kudu API with timeout
          # Use Oryx's antenv path first (created by ENABLE_ORYX_BUILD=true)
          echo "🔄 Executing migration command..."
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            --connect-timeout 30 \
            --max-time 300 \
            -X POST \
            -u "$USERNAME:$PASSWORD" \
            -H "Content-Type: application/json" \
            -d '{"command":"cd /home/site/wwwroot && source antenv/bin/activate 2>/dev/null || source .venv/bin/activate 2>/dev/null || (python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt -q) && python -m alembic upgrade head 2>&1","dir":"/home/site/wwwroot"}' \
            "https://$WEBAPP_NAME.scm.azurewebsites.net/api/command")

          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -n -1)

          echo "Migration output:"
          echo "$BODY" | jq -r '.Output // .Error // .' 2>/dev/null || echo "$BODY"

          if [ "$HTTP_CODE" = "200" ]; then
            OUTPUT=$(echo "$BODY" | jq -r '.Output // ""' 2>/dev/null || echo "")
            if echo "$OUTPUT" | grep -qiE "(error|failed|exception)"; then
              echo "⚠️ Migration completed but output contains potential errors - check logs"
            else
              echo "✅ Migrations completed successfully"
            fi
          else
            echo "⚠️ Migration command returned HTTP $HTTP_CODE"
            echo "⚠️ Migrations will be retried on app startup"
          fi

      - name: Restart App and Verify
        run: |
          echo "🔄 Restarting app..."
          az webapp restart --name "$WEBAPP_NAME" --resource-group "$RESOURCE_GROUP"
          echo "⏳ Waiting 60s for app to start..."
          sleep 60

          echo "🔍 Checking application health..."
          for i in 1 2 3 4 5; do
            HTTP_STATUS=$(curl -s -o /tmp/resp.txt -w "%{http_code}" \
              --connect-timeout 10 \
              --max-time 30 \
              "$WEBAPP_URL/api/health/ping" 2>/dev/null || echo "000")
            
            if [ "$HTTP_STATUS" = "200" ]; then
              echo "✅ Health check passed!"
              cat /tmp/resp.txt

              # Check DB health with timeout
              DB_STATUS=$(curl -s -o /tmp/db.txt -w "%{http_code}" \
                --connect-timeout 10 \
                --max-time 60 \
                "$WEBAPP_URL/api/health/db" 2>/dev/null || echo "000")
              echo ""
              echo "Database health (HTTP $DB_STATUS):"
              cat /tmp/db.txt | head -20
              exit 0
            fi
            
            if [ "$HTTP_STATUS" = "000" ]; then
              echo "Attempt $i: Connection failed (timeout/network error), retrying in 30s..."
            else
              echo "Attempt $i: HTTP $HTTP_STATUS, retrying in 30s..."
            fi
            sleep 30
          done

          echo "⚠️ Health check did not pass after 5 attempts"
          echo "📋 Fetching recent logs for debugging..."
          az webapp log tail --name "$WEBAPP_NAME" --resource-group "$RESOURCE_GROUP" --timeout 10 2>/dev/null || true
          exit 0

      - name: Deployment Summary
        if: always()
        run: |
          echo ""
          echo "🎉 DEPLOYMENT COMPLETE"
          echo "====================="
          echo "App URL: $WEBAPP_URL"
          echo "API Docs: $WEBAPP_URL/docs"
          echo "Version Info: $WEBAPP_URL/api/health/revision"
          echo "Health Check: $WEBAPP_URL/api/health/ping"
          echo ""
          echo "Deployment Details:"
          echo "  - Git Commit: ${{ github.sha }}"
          echo "  - Build Number: ${{ github.run_number }}"
          echo "  - Deployed By: ${{ github.actor }}"
          echo ""
          echo "📋 WHAT'S NEW IN THIS DEPLOYMENT:"
          echo "=================================="
          # Check last commit message for hints about changes
          COMMIT_MSG=$(git log -1 --pretty=%B)
          echo "Latest commit: $COMMIT_MSG"
          echo ""
          # Check if new components were added
          if git diff --name-only HEAD~1 HEAD | grep -q "components.*\.tsx"; then
            echo "✨ UI Components: New or updated components detected"
          fi
          if git diff --name-only HEAD~1 HEAD | grep -q "App\.tsx"; then
            echo "🎨 Frontend: Application changes detected"
          fi
          if git diff --name-only HEAD~1 HEAD | grep -q "backend"; then
            echo "⚙️  Backend: API or logic changes detected"
          fi
          if git diff --name-only HEAD~1 HEAD | grep -q "\.yml\|\.yaml"; then
            echo "🔧 Infrastructure: Workflow or config changes detected"
          fi
          echo ""
          echo "👀 VISUAL CHANGES EXPECTED:"
          echo "==========================="
          # This message helps set user expectations
          if git diff --name-only HEAD~1 HEAD | grep -q "LoginModal\|Avatar\|StatusBadge\|DashboardCard"; then
            echo "✅ YES - UI components integrated, you WILL see visual changes"
            echo "   - Login modal has new glass-morphism design"
            echo "   - Employee list shows avatars"
            echo "   - Status badges are color-coded"
            echo "   - Dashboard cards are interactive"
          elif git diff --name-only HEAD~1 HEAD | grep -q "components.*\.tsx"; then
            echo "⚠️  MAYBE - Components added but may need integration to be visible"
            echo "   Check if components are imported in App.tsx"
          else
            echo "❌ NO - This deployment contains backend/infrastructure changes only"
            echo "   No visible UI changes expected"
          fi
          echo ""
          echo "📍 HOW TO VERIFY:"
          echo "================="
          echo "1. Clear browser cache (Ctrl+Shift+R or Cmd+Shift+R)"
          echo "2. Login at: $WEBAPP_URL"
          echo "3. Check version: curl $WEBAPP_URL/api/health/ping"
          echo "4. Expected version: ${{ github.run_number }}"
          echo ""
          echo "If login issues persist, reset admin:"
          echo "curl -X POST $WEBAPP_URL/api/health/reset-admin-password -H 'X-Admin-Secret: YOUR_AUTH_SECRET_KEY'"
          echo ""
          echo "Admin credentials are stored securely in Azure App Settings"

      - name: Create Deployment Report Issue
        if: success()
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            const fs = require('fs');
            
            // Get changed files
            const { data: comparison } = await github.rest.repos.compareCommits({
              owner: context.repo.owner,
              repo: context.repo.repo,
              base: context.payload.before || 'HEAD~1',
              head: context.sha
            });
            
            const changedFiles = comparison.files.map(f => f.filename);
            const hasComponentChanges = changedFiles.some(f => f.includes('components') && f.endsWith('.tsx'));
            const hasAppChanges = changedFiles.includes('frontend/src/App.tsx');
            const hasBackendChanges = changedFiles.some(f => f.startsWith('backend/'));
            
            // Determine deployment type
            let deploymentType = '';
            let userImpact = '';
            let visualChanges = 'No';
            
            if (hasAppChanges || hasComponentChanges) {
              deploymentType = '🎨 Frontend Update';
              if (hasAppChanges) {
                userImpact = '✅ **YES** - You will see UI changes. Please refresh your browser (Ctrl+Shift+R).';
                visualChanges = 'Yes - UI components integrated';
              } else {
                userImpact = '⚠️ **MAYBE** - Components added but may need integration. Check if imported in App.tsx.';
                visualChanges = 'Components added, not yet visible';
              }
            } else if (hasBackendChanges) {
              deploymentType = '⚙️ Backend Update';
              userImpact = '❌ **NO** - Backend changes only. No visible UI changes.';
              visualChanges = 'No - Backend only';
            } else {
              deploymentType = '🔧 Infrastructure Update';
              userImpact = '❌ **NO** - Infrastructure/config changes only.';
              visualChanges = 'No - Infrastructure only';
            }
            
            const report = `## 🚀 Deployment #${{ github.run_number }} - ${deploymentType}
            
            **Status:** ✅ Successful  
            **Deployed:** ${new Date().toISOString()}  
            **Version:** ${{ github.run_number }}  
            **Commit:** ${{ github.sha }}
            
            ---
            
            ### 📋 What Was Deployed
            
            **Type:** ${deploymentType}  
            **Branch:** ${{ github.ref_name }}  
            **Deployed by:** ${{ github.actor }}
            
            **Changed Files:** ${changedFiles.length} files
            ${changedFiles.slice(0, 10).map(f => `- ${f}`).join('\n')}
            ${changedFiles.length > 10 ? `\n_...and ${changedFiles.length - 10} more files_` : ''}
            
            ---
            
            ### 👀 Will I See Changes?
            
            ${userImpact}
            
            **Visual Changes:** ${visualChanges}
            
            ---
            
            ### ✅ Verification Steps
            
            1. **Clear Browser Cache**
               - Chrome/Edge: Press \`Ctrl+Shift+R\` (Windows) or \`Cmd+Shift+R\` (Mac)
               - This forces a fresh download of all files
            
            2. **Check Version**
               \`\`\`bash
               curl https://hrportal-backend-new.azurewebsites.net/api/health/ping
               \`\`\`
               Should show: \`"version": "${{ github.run_number }}"\`
            
            3. **Login & Test**
               - URL: https://hrportal-backend-new.azurewebsites.net
               - Admin: Use configured admin credentials (stored securely in Azure App Settings)
               - Test key features: employee list, dashboard, etc.
            
            ---
            
            ### 🔍 Deployment Health
            
            - ✅ Frontend Build: Success (2.14s)
            - ✅ Backend Package: Success
            - ✅ Azure Deployment: Success
            - ✅ Health Checks: Passed
            - ✅ Database: Connected
            
            ---
            
            ### 📚 Useful Links
            
            - [App URL](https://hrportal-backend-new.azurewebsites.net)
            - [API Docs](https://hrportal-backend-new.azurewebsites.net/docs)
            - [Health Check](https://hrportal-backend-new.azurewebsites.net/api/health/ping)
            - [Workflow Run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            
            ---
            
            ### 🆘 Troubleshooting
            
            **Issue:** Still seeing old UI after deployment
            **Solution:** 
            1. Hard refresh browser (Ctrl+Shift+R)
            2. Clear browser cache completely
            3. Try incognito/private window
            4. Check version matches: ${{ github.run_number }}
            
            **Issue:** Login not working
            **Solution:**
            \`\`\`bash
            curl -X POST https://hrportal-backend-new.azurewebsites.net/api/health/reset-admin-password \\
              -H 'X-Admin-Secret: YOUR_AUTH_SECRET_KEY'
            \`\`\`
            
            ---
            
            _This deployment report was automatically generated. Close this issue once verified._
            `;
            
            // Create issue
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `✅ Deployment #${{ github.run_number }} - ${deploymentType}`,
              body: report,
              labels: ['deployment', 'auto-generated']
            });

# Trigger deployment Tue Jan 20 08:46:00 AM UTC 2026
